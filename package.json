{
  "name": "devtool-loop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DevTool Discovery & Build Loop — Box Content Hub (Person B)",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "setup:phase0": "node --env-file=.env setup/phase0.mjs"
  },
  "dependencies": {
    "box-typescript-sdk-gen": "^1.19.1"
  }
}
